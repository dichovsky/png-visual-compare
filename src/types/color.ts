/** An RGB colour used to paint pixels on a PNG buffer. All channels are in the range 0–255. */
export type Color = {
  /** Red channel (0–255). */
  r: number;
  /** Green channel (0–255). */
  g: number;
  /** Blue channel (0–255). */
  b: number;
};
