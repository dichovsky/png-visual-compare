# Claude Memory

This directory holds project-specific mistake logs written by coding agents (see the Mistake Logging section of `CLAUDE.md`) during development sessions.

Individual memory files (e.g. `*.md` session logs) are gitignored — only this README is committed.
Add a new file here to record patterns, mistakes, or context that should persist across sessions for this project.
